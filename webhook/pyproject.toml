[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "Ava-webhook"
description = "Grabs API requests sent in from Freshservice for Ava."
version = "0.1"
requires-python = ">=3.12"

[project.scripts]
webhook = "app.main:webhook"
